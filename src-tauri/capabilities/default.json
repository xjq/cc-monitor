{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the overlay and detail windows",
  "windows": ["overlay", "detail"],
  "permissions": [
    "core:default"
  ]
}
